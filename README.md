Sources for the plugin `plugin.appodeal.beta.Flurry`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Flurry" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
